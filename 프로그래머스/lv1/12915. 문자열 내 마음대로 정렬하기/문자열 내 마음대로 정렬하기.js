function solution(strings, n) {
    let temp = []
    for(any of strings) {
        temp.push(any[n] + any)
    }
    temp.sort();
    let arr = []
    for (any of temp) {
        arr.push(any.slice(1));
    }

    return arr;
}