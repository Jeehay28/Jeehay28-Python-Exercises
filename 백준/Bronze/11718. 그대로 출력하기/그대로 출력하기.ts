let input11718 = require("fs").readFileSync(0, "utf8").trim().split("\n");

const arr11718: string[] = input11718.map((str: string) => str.trim());

arr11718.forEach((item) => {
  console.log(item);
});
